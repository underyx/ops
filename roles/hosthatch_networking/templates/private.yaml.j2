network:
    version: 2
    ethernets:
        {{ private_network_interface }}:
            addresses:
            - {{ private_ip }}
            match:
                macaddress: {{ private_macaddress }}
            mtu: 9000  # jumbo frames work on hosthatch private networking
