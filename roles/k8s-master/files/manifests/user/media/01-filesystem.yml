---
apiVersion: ceph.rook.io/v1beta1
kind: Filesystem
metadata:
  name: media-fs
  namespace: rook-ceph
spec:
  metadataPool:
    replicated:
      size: 1
  dataPools:
    - replicated:
        size: 1
  metadataServer:
    activeCount: 1
    activeStandby: true
